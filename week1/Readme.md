workshop1.1

สร้างตัวแปรเพื่อเก็บข้อมูล ชื่อ, อายุ, สาขา
จากนั้นแสดงผลของข้อมูลโดยใช้คำสั่ง console.log()

workshop1.2

สร้างตัวแปรโดยกำหนดประเภทของตัวแปรเพื่อเก็บข้อมูลที่มีค่าแตกต่างกัน 
จากนั้นแสดงผลผ่านคำสั่ง console.log() โดยใช้ typeof ตรวจสอบชนิดของตัวแปร

workshop1.3

สร้างตัวแปรเพื่อเก็บคะแนนของแต่ละวิชา 
จากนั้นนำคะแนนทั้งหมดมารวมกันแล้วหาค่าเฉลี่ยโดยนำคะแนนรวมมาหารด้วยจำนวนวิชา
แล้วใช้ if เพื่อตรวจสอบค่าเฉลี่ยหากมากกว่าหรือเท่ากับ 80 ให้แสดงผลลัพธ์เป็น true

workshop1.4

สร้างตัวแปรเก็บ username, password และ age
ใช้ if-else if เพื่อตรวจสอบเงื่อนไข หาก username คือ admin, password คือ 1234, age >=18 ให้แสดง "เข้าสู่ระบบสำเร็จ"
ถ้า username ,password ไม่ถูกต้องให้แสดง "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง"
ถ้า age <18 ให้แสดง "อายุไม่ถึงเกณฑ์"
